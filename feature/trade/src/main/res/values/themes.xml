<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ExampleShop" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:hint">What are you looking for ?</item>
        <item name="android:textColorHint">@color/color5B5B5B</item>
        <item name="android:queryHint">What are you looking for ?</item>
        <item name="android:textColor">@color/color5B5B5B</item>
    </style>
</resources>